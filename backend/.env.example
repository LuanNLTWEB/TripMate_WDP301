# Server
PORT=5000
NODE_ENV=development

# MongoDB Atlas (required)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/tripmate_WDP301?retryWrites=true&w=majority
DB_NAME=tripmate_WDP301

# JWT (required)
JWT_SECRET=<replace-with-a-long-random-secret>
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:5173